AWSTemplateFormatVersion: '2010-09-09'
Transform:
  - AWS::LanguageExtensions
  - AWS::Serverless-2016-10-31
Description: Public read-only job discovery API with authenticated personal workflows

Parameters:
  RecordsTableName:
    Type: String
    Default: job-discovery-records
  ListingsTableName:
    Type: String
    Default: job-discovery-listings
  AllowedOrigin:
    Type: String
    Description: CloudFront dashboard origin, for example https://jobs.example.com
  CallbackUrl:
    Type: String
    Description: Cognito callback URL, for example https://jobs.example.com/auth/callback
  LogoutUrl:
    Type: String
    Description: Cognito logout URL, for example https://jobs.example.com/jobs
  CognitoDomainPrefix:
    Type: String
    Description: Globally unique prefix for the Cognito managed login domain
  LambdaArtifactBucket:
    Type: String
    Description: S3 bucket containing the dashboard Lambda deployment package
  DashboardLambdaCodeKey:
    Type: String
    Description: Immutable S3 key for the dashboard Lambda deployment package
  ScoringLambdaCodeKey:
    Type: String
    Description: Immutable S3 key for the personalized scoring Lambda deployment package
  SimplifyLambdaCodeKey:
    Type: String
    Description: Immutable S3 key for the Simplify New Grad crawler Lambda deployment package
  GeminiApiKey:
    Type: String
    NoEcho: true
  GeminiModel:
    Type: String
  DedupKeysTableName:
    Type: String
    Default: job-discovery-dedup-keys
  SourceLookupTableName:
    Type: String
    Default: job-discovery-source-lookup
  WorkdayFunctionName:
    Type: String
    Default: job-discovery-workday
  JobSpyFunctionName:
    Type: String
    Default: job-discovery-jobspy

Resources:
  DashboardUserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: job-dashboard-users
      UsernameAttributes:
        - email
      AutoVerifiedAttributes:
        - email
      AdminCreateUserConfig:
        AllowAdminCreateUserOnly: true
      Policies:
        PasswordPolicy:
          MinimumLength: 12
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: true
          RequireUppercase: true

  DashboardUserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      ClientName: job-dashboard-spa
      UserPoolId: !Ref DashboardUserPool
      GenerateSecret: false
      SupportedIdentityProviders:
        - COGNITO
      AllowedOAuthFlowsUserPoolClient: true
      AllowedOAuthFlows:
        - code
      AllowedOAuthScopes:
        - openid
        - email
        - profile
      CallbackURLs:
        - !Ref CallbackUrl
      LogoutURLs:
        - !Ref LogoutUrl
      PreventUserExistenceErrors: ENABLED
      EnableTokenRevocation: true
      AccessTokenValidity: 1
      IdTokenValidity: 1
      RefreshTokenValidity: 7
      TokenValidityUnits:
        AccessToken: hours
        IdToken: hours
        RefreshToken: days

  DashboardUserPoolDomain:
    Type: AWS::Cognito::UserPoolDomain
    Properties:
      Domain: !Ref CognitoDomainPrefix
      UserPoolId: !Ref DashboardUserPool

  DashboardUserDataTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: job-discovery-user-data
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
        - AttributeName: entity_key
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH
        - AttributeName: entity_key
          KeyType: RANGE

  DashboardApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: '$default'
      CorsConfiguration:
        AllowOrigins:
          - !Ref AllowedOrigin
        AllowHeaders:
          - Authorization
          - Content-Type
        AllowMethods:
          - GET
          - POST
          - PUT
          - DELETE
        MaxAge: 600
      DefaultRouteSettings:
        ThrottlingBurstLimit: 5
        ThrottlingRateLimit: 2
      Auth:
        DefaultAuthorizer: CognitoJwtAuthorizer
        Authorizers:
          CognitoJwtAuthorizer:
            IdentitySource: '$request.header.Authorization'
            JwtConfiguration:
              issuer: !Sub https://cognito-idp.${AWS::Region}.amazonaws.com/${DashboardUserPool}
              audience:
                - !Ref DashboardUserPoolClient

  SimplifyDiscoveryFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: job-discovery-simplify
      CodeUri:
        Bucket: !Ref LambdaArtifactBucket
        Key: !Ref SimplifyLambdaCodeKey
      Handler: lambda_function.lambda_handler
      Runtime: python3.13
      Architectures:
        - x86_64
      MemorySize: 512
      Timeout: 900
      Environment:
        Variables:
          RECORDS_TABLE: !Ref RecordsTableName
          LISTINGS_TABLE: !Ref ListingsTableName
          DEDUP_KEYS_TABLE: !Ref DedupKeysTableName
          SOURCE_LOOKUP_TABLE: !Ref SourceLookupTableName
          USER_DATA_TABLE: !Ref DashboardUserDataTable
      Policies:
        - Statement:
            - Sid: ReadWriteDiscoveryTables
              Effect: Allow
              Action:
                - dynamodb:GetItem
                - dynamodb:PutItem
                - dynamodb:Query
                - dynamodb:Scan
                - dynamodb:UpdateItem
              Resource:
                - !Sub arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${RecordsTableName}
                - !Sub arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${ListingsTableName}
                - !Sub arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${DedupKeysTableName}
                - !Sub arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${SourceLookupTableName}
                - !GetAtt DashboardUserDataTable.Arn
      Events:
        DailyNewGradDiscovery:
          Type: ScheduleV2
          Properties:
            Name: job-discovery-simplify-managed
            Description: Run the two curated new-grad feeds once per Toronto day
            ScheduleExpression: cron(30 10 * * ? *)
            ScheduleExpressionTimezone: America/Toronto
            FlexibleTimeWindow:
              Mode: 'OFF'
            State: ENABLED
            Input: '{}'
            RetryPolicy:
              MaximumEventAgeInSeconds: 3600
              MaximumRetryAttempts: 1

  DashboardReadFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: job-dashboard-read
      CodeUri:
        Bucket: !Ref LambdaArtifactBucket
        Key: !Ref DashboardLambdaCodeKey
      Handler: lambda_function.lambda_handler
      Runtime: python3.13
      Architectures:
        - x86_64
      MemorySize: 1024
      Timeout: 10
      Environment:
        Variables:
          RECORDS_TABLE: !Ref RecordsTableName
          LISTINGS_TABLE: !Ref ListingsTableName
          USER_DATA_TABLE: !Ref DashboardUserDataTable
          ALLOWED_ORIGIN: !Ref AllowedOrigin
          DASHBOARD_CACHE_TTL_SECONDS: '300'
          REQUIRE_AUTH: 'true'
          SCORING_FUNCTION_NAME: !Ref PersonalizedScoringFunction
          WORKDAY_FUNCTION_NAME: !Ref WorkdayFunctionName
          JOBSPY_FUNCTION_NAME: !Ref JobSpyFunctionName
          SIMPLIFY_FUNCTION_NAME: !Ref SimplifyDiscoveryFunction
      Policies:
        - Statement:
            - Sid: ReadDashboardTables
              Effect: Allow
              Action:
                - dynamodb:GetItem
                - dynamodb:Query
                - dynamodb:Scan
              Resource:
                - !Sub arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${RecordsTableName}
                - !Sub arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${ListingsTableName}
            - Sid: ReadWriteDashboardUserState
              Effect: Allow
              Action:
                - dynamodb:GetItem
                - dynamodb:PutItem
                - dynamodb:Query
                - dynamodb:UpdateItem
              Resource: !GetAtt DashboardUserDataTable.Arn
            - Sid: InvokePersonalizedScoring
              Effect: Allow
              Action:
                - lambda:InvokeFunction
              Resource: !GetAtt PersonalizedScoringFunction.Arn
            - Sid: InvokeDiscoveryCrawlers
              Effect: Allow
              Action:
                - lambda:InvokeFunction
              Resource:
                - !Sub arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:${WorkdayFunctionName}
                - !Sub arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:${JobSpyFunctionName}
                - !GetAtt SimplifyDiscoveryFunction.Arn
      Events:
        GetBootstrap:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: GET
            Path: /bootstrap
        ListJobs:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: GET
            Path: /jobs
            Auth:
              Authorizer: NONE
        GetJob:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: GET
            Path: /jobs/{job_id}
            Auth:
              Authorizer: NONE
        ListRuns:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: GET
            Path: /runs
            Auth:
              Authorizer: NONE
        GetScoringQueue:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: GET
            Path: /scoring/queue
        ScoreJobNow:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: POST
            Path: /jobs/{job_id}/score
        ScoreRunNow:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: POST
            Path: /actions/scoring
        RunCrawlerNow:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: POST
            Path: /actions/crawler
        GetUserState:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: GET
            Path: /user-state
        MarkJobViewed:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: PUT
            Path: /jobs/{job_id}/viewed
        UpdateJobState:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: PUT
            Path: /jobs/{job_id}/state
        MarkRunViewed:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: PUT
            Path: /runs/{run_id}/viewed
        ListBlockedCompanies:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: GET
            Path: /blocked-companies
        BlockCompany:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: POST
            Path: /blocked-companies
        UnblockCompany:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: DELETE
            Path: /blocked-companies/{company}
        GetScoringProfile:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: GET
            Path: /profile/scoring
        UpdateScoringProfile:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: PUT
            Path: /profile/scoring
        GetDiscoverySettings:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: GET
            Path: /settings/discovery
        UpdateDiscoverySettings:
          Type: HttpApi
          Properties:
            ApiId: !Ref DashboardApi
            Method: PUT
            Path: /settings/discovery

  PersonalizedScoringFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: job-discovery-score
      CodeUri:
        Bucket: !Ref LambdaArtifactBucket
        Key: !Ref ScoringLambdaCodeKey
      Handler: lambda_function.lambda_handler
      Runtime: python3.13
      Architectures:
        - x86_64
      MemorySize: 256
      Timeout: 900
      Environment:
        Variables:
          RECORDS_TABLE: !Ref RecordsTableName
          LISTINGS_TABLE: !Ref ListingsTableName
          DEDUP_KEYS_TABLE: !Ref DedupKeysTableName
          SOURCE_LOOKUP_TABLE: !Ref SourceLookupTableName
          USER_DATA_TABLE: !Ref DashboardUserDataTable
          GEMINI_API_KEY: !Ref GeminiApiKey
          GEMINI_MODEL: !Ref GeminiModel
      Policies:
        - Statement:
            - Sid: ScoreJobRecords
              Effect: Allow
              Action:
                - dynamodb:GetItem
                - dynamodb:Scan
                - dynamodb:UpdateItem
              Resource: !Sub arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${RecordsTableName}
            - Sid: ReadWritePersonalizedScores
              Effect: Allow
              Action:
                - dynamodb:GetItem
                - dynamodb:PutItem
                - dynamodb:Query
                - dynamodb:Scan
                - dynamodb:UpdateItem
              Resource: !GetAtt DashboardUserDataTable.Arn
      Events:
        ScheduledScoring:
          Type: ScheduleV2
          Properties:
            ScheduleExpression: cron(45 10 * * ? *)
            ScheduleExpressionTimezone: America/Toronto
            Input: '{"limit":30,"max_age_days":2}'

Outputs:
  ApiUrl:
    Value: !Sub https://${DashboardApi}.execute-api.${AWS::Region}.${AWS::URLSuffix}
  UserPoolId:
    Value: !Ref DashboardUserPool
  UserPoolClientId:
    Value: !Ref DashboardUserPoolClient
  CognitoLoginDomain:
    Value: !Sub https://${CognitoDomainPrefix}.auth.${AWS::Region}.amazoncognito.com
  UserDataTableName:
    Value: !Ref DashboardUserDataTable
  PersonalizedScoringFunctionName:
    Value: !Ref PersonalizedScoringFunction
