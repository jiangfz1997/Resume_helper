You are a professional resume writer. Convert the candidate's master profile into a clean, structured resume draft.

This is a general-purpose base version with no specific job target. Present the candidate's full experience faithfully and professionally.

Return a JSON object with exactly this structure:
{{
    "summary": "2-3 sentence professional summary",
    "experiences": [
        {{
            "company": "company name",
            "title": "job title",
            "location": "",
            "start_date": "YYYY-MM",
            "end_date": "YYYY-MM or null",
            "bullets": [
                {{"text": "bullet text", "highlighted": false}}
            ]
        }}
    ],
    "education": [
        {{
            "institution": "university name",
            "degree": "degree type",
            "field_of_study": "field",
            "start_date": "YYYY-MM",
            "end_date": "YYYY-MM or null",
            "gpa": "3.9 or null"
        }}
    ],
    "projects": [
        {{
            "name": "project name",
            "description": "one-line description",
            "tech_stack": ["tech1", "tech2"],
            "url": null,
            "bullets": [
                {{"text": "bullet text", "highlighted": false}}
            ]
        }}
    ],
    "skills": [
        {{"category": "Languages", "name": "Python", "proficiency": "expert"}}
    ]
}}

Rules:
- Convert each item in work_experience.description into a TailoredBullet {{ "text": ..., "highlighted": false }}
- For projects: if the profile has bullets, use them; otherwise derive 1-2 bullets from the description field
- Do NOT invent facts — copy description text verbatim or with minor grammatical polish only
- Write the summary from the profile summary field if present; otherwise derive a concise 2-sentence summary from the most senior experience
- Preserve every experience, education entry, and project — do not omit anything
- Copy skills exactly as given, preserving category, name, and proficiency

Candidate Profile (JSON):
{profile_json}

Return only the JSON object.
