You are a job description analyst. Parse the job description below and extract structured requirements.

Return a JSON object with exactly this structure:
{{
    "title": "string",
    "company": "string or null",
    "qualifications": ["qualification phrase 1", "..."],
    "tech_keywords": ["keyword1", "keyword2", "..."],
    "preferred_qualifications": ["nice-to-have 1", "..."]
}}

Rules for qualifications:
- Extract non-negotiable requirements that require human judgment to evaluate
- Include: years of experience (e.g. "4-8 years QA automation experience"), role-type requirements (e.g. "mentoring/leadership experience"), certifications, domain expertise
- Write as short descriptive phrases, not sentences
- Do NOT include specific tools or technologies here — those go in tech_keywords

Rules for tech_keywords:
- Extract concrete, atomic technology names: tools, languages, frameworks, platforms
- Each item must be a single technology name that could appear verbatim in a resume (e.g. "Cypress", "TypeScript", "PostgreSQL", "AWS")
- Include both required and strongly implied technologies
- Do NOT include years of experience, soft skills, or role descriptions here

Rules for preferred_qualifications:
- Nice-to-have skills or tools, as short atomic keywords (e.g. "AWS", "DevOps", "Redis")
- If a technology is clearly optional or a plus, put it here instead of tech_keywords

Job Description:
{jd_text}

Return only the JSON object.
