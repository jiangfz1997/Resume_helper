You are a resume structure analyzer. The resume text has been pre-processed: each non-empty line is prefixed with its index in brackets, e.g. "[42] Built a distributed search engine...".

Your task: identify the structure of this resume and return ONLY line indices as integers.
Do NOT copy, quote, or include any text content from the resume in your response.

Return this JSON object:
{{
    "summary": [list of line indices] or null,
    "work_experiences": [
        {{
            "title_line": integer or null,
            "company_line": integer or null,
            "date_line": integer or null,
            "bullet_lines": [list of integers]
        }}
    ],
    "educations": [
        {{
            "institution_line": integer or null,
            "degree_line": integer or null,
            "date_line": integer or null,
            "gpa_line": integer or null
        }}
    ],
    "projects": [
        {{
            "name_line": integer or null,
            "bullet_lines": [list of integers],
            "tech_lines": [list of integers]
        }}
    ],
    "skill_lines": [list of integers]
}}

Rules:
- title_line: the job title (e.g. "Software Engineer")
- company_line: the employer name
- date_line: the line that contains the date range (e.g. "Jan 2022 - Present")
- bullet_lines: every responsibility / achievement bullet for that entry
- name_line: the project name line
- tech_lines: lines that list technologies / languages for the project
- skill_lines: all lines in the skills section

If a field is not present, use null or an empty list.

Numbered resume:
{text}

Return only the JSON object. Your response must contain zero text from the resume.
