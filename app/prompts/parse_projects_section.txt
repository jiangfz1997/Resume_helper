You are extracting project entries from a resume section.

ABSOLUTE RULE: Copy all bullet text CHARACTER-FOR-CHARACTER from the input.
Do NOT rephrase, shorten, expand, or change a single word.

Think through the following steps before producing output:

Step 1 — Find project boundaries.
  Projects are separated by their names/titles. A project name is typically a short noun phrase
  at the start of an entry, not a bullet and not a date. It may be followed by a URL, tech stack
  list, or description line before the bullets begin.

Step 2 — For each project, classify lines.
  - name line: the short title of the project.
  - description line: a one-sentence overview (if present before bullets). Copy verbatim.
  - bullet lines: contribution/feature sentences. Often start with a verb or "•".
  - tech lines: lines listing technologies (e.g. "Python | PostgreSQL | Redis" or "Tech: ...").
  - url: a GitHub/demo link if present.

Step 3 — Copy bullets verbatim.
  Include EVERY bullet line for each project in the "bullets" array.
  Copy each bullet EXACTLY as written — do not omit, merge, split, or rephrase any bullet.

Step 4 — Extract tech stack.
  Split tech lines on commas, pipes, or spaces to produce individual technology names.

Return a JSON object with key "items" containing the array of entries:
{{
  "items": [
    {{
      "name":        "string",
      "description": "first description line copied verbatim, or empty string",
      "bullets":     ["exact bullet text", "exact bullet text"],
      "tech_stack":  ["tech1", "tech2"],
      "url":         "string or null"
    }}
  ]
}}

Projects section text:
{section_text}

Return only the JSON array.
