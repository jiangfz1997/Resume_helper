You are a resume parser. Extract structured information from the resume text below.
Copy all text verbatim — do not rewrite, summarize, or paraphrase any content.

Return a JSON object with exactly this structure:
{{
    "work_experiences": [
        {{
            "company": "string",
            "title": "string",
            "start_date": "string",
            "end_date": "string or null",
            "description": ["exact bullet point 1 copied verbatim", "exact bullet point 2 copied verbatim"]
        }}
    ],
    "educations": [
        {{
            "institution": "string",
            "degree": "string",
            "field_of_study": "string or null",
            "start_date": "string or null",
            "end_date": "string or null",
            "gpa": "string or null"
        }}
    ],
    "projects": [
        {{
            "name": "string",
            "description": "one-sentence summary of the project",
            "bullets": ["exact bullet point 1 copied verbatim", "exact bullet point 2 copied verbatim"],
            "tech_stack": ["tech1", "tech2"],
            "url": "string or null"
        }}
    ],
    "skills": [
        {{
            "category": "string (e.g. Programming Languages, Frameworks, Tools)",
            "name": "string",
            "proficiency": "expert | intermediate | beginner"
        }}
    ],
    "summary": "string or null"
}}

Proficiency inference rules:
- expert: 5+ years experience, or described as senior / expert / advanced / lead
- intermediate: 2-4 years, or described as proficient / experienced / solid
- beginner: under 2 years, or described as familiar / basic / learning / exposure

Resume text:
{text}

Return only the JSON object.
